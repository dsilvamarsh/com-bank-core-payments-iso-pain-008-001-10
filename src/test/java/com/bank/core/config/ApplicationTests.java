package com.bank.core.config;

import org.springframework.boot.test.context.SpringBootTest;


@SpringBootTest
class ApplicationTests {

	
	/*
	 * @Autowired private RestTemplate client; private static final String
	 * personUrl="http://localhost:8080/person";
	 * 
	 * @Test void contextLoads() { ResponseEntity<Person> p =
	 * client.getForEntity(personUrl, Person.class);
	 * System.out.println(p.getBody().getEmail());
	 * System.out.println(p.getBody().getName());
	 * System.out.println(p.getBody().getId()); }
	 */

}
